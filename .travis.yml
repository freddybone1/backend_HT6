language: python
python:
  - "3.8"
# command to install dependencies
install:
  - pip install Poetry
#  - pip install -r requirements.txt
  - Poetry install
# command to run tests
script:
  - flake8 backend_HT5 --exclude=venv